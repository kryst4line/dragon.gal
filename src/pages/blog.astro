---
import IndexLayout from '@/layouts/IndexLayout.astro'
import About from '@/components/widgets/About.astro'
import PostList from '@/components/widgets/PostList.astro'
import { themeConfig } from '@/config'
import { getGroupedSortedFilteredPosts, getSortedFilteredPosts } from '@/utils/draft'
import GroupedPostList from '@/components/widgets/GroupedPostList.astro'

const groupedPosts = await getGroupedSortedFilteredPosts();

---
<IndexLayout title={'blog | ' + themeConfig.site.title} description={themeConfig.site.description}>
  <GroupedPostList groupedPosts={groupedPosts} />
</IndexLayout>

<style>

</style>